#include "main.h"

/**
 * add - add two integers
 * @a: one of the nubers to add
 * @b: one of the nubers to add
 * Return: result of the addition
 */
int add(int a, int b)
{
	int result = a + b;

	return (result);
}
